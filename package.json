{
  "private": true,
  "scripts": {
    "build": "vite build --base ./ --emptyOutDir --outDir ../dist src",
    "fmt": "prettier -u --write '**/*'",
    "start": "vite src --open",
    "test": "prettier --check -u '**/*' && tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.3",
    "vite": "^7.2.2"
  }
}
